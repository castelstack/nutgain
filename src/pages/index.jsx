import React from 'react';
import Home from '../containers/homepageContainer';

const Homepage = () => {
    return (
        <div>
            <Home/>
        </div>
    );
};

export default Homepage;